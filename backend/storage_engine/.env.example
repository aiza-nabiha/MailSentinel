# Required by the Flask API. Store the actual value outside version control.
API_SECRET_KEY=replace-with-a-long-random-secret

# Optional: enables encrypted local archival of submitted raw email data.
DB_ENCRYPTION_KEY=generate-with-python3-crypto-py
